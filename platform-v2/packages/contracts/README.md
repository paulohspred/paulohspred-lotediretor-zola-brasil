# @lotediretor/contracts

Contratos versionados OpenAPI/GraphQL/dataset. Nenhum frontend deve depender diretamente do banco.

Primeiros contratos a implementar:
- `/api/v1/parcel/*`
- `/api/v1/legal/*`
- `/api/v1/analysis/*`
- `/graphql` para composição da aplicação
- MVT `/tiles/{layer}/{z}/{x}/{y}.pbf`
- SSE `/events/jobs/{id}`
